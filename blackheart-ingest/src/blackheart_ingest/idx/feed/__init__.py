"""Stockbit datafeed collector: real-time prints and order books from the operator's own Stockbit session into
``idx.feed_*`` (migration 0029). See ``collector.py`` for the lifecycle, ``proto.py`` for the wire format."""
